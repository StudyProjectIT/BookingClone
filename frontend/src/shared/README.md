# shared layer

Reusable code with no business knowledge:
- `api/`     — HTTP client, request helpers
- `ui/`      — design-system primitives (Button, Input, ...)
- `lib/`     — pure utilities, helpers
- `config/`  — env, runtime config, constants
- `assets/`  — static assets (svg, png, fonts)
